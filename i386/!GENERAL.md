i386 or x32 is a different architect from x86_64

some main differences include:

- argument passed to functions are pushed into stack instead of passing into registers
- only 32 bits registers exist (edi, eax, esp, etc.)